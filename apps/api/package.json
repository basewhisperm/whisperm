{
  "name": "@whisperm/api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm --filter @whisperm/types build && pnpm --filter @whisperm/billing-runtime build && pnpm run build && node --test test/*.test.mjs test/*.test.js"
  },
  "dependencies": {
    "@whisperm/billing-runtime": "workspace:^",
    "@whisperm/notification-runtime": "workspace:^",
    "@whisperm/types": "workspace:*",
    "fastify-raw-body": "^5.0.0",
    "paystack-node": "^0.3.0",
    "stripe": "^22.2.0"
  }
}
