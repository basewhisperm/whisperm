{
  "name": "@whisperm/worker",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "pnpm run build && node --test test/*.test.js test/*.test.mjs test/smoke.test.js"
  },
  "dependencies": {
    "@whisperm/types": "workspace:*",
    "@whisperm/worker-runtime": "workspace:*",
    "zod": "3.25.76"
  }
}
