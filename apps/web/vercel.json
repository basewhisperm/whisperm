{
  "buildCommand": "pnpm --filter @whisperm/types build && pnpm build",
  "outputDirectory": ".next",
  "framework": "nextjs",
  "crons": [
    {
      "path": "/api/internal/queue-drain",
      "schedule": "*/5 * * * *"
    }
  ]
}
