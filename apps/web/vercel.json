{
  "buildCommand": "pnpm --filter @whisperm/types build && ./node_modules/.bin/prisma generate --schema=../../prisma/schema.prisma && pnpm build",
  "outputDirectory": ".next",
  "framework": "nextjs"
}
